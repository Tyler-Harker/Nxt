@inherits global::Microsoft.AspNetCore.Components.LayoutComponentBase
<aside style="background:#1e3a5f;color:#dbeafe;padding:.5rem 1rem;margin-bottom:1rem;border-radius:4px">
    📚 <strong>Blog</strong> — this banner comes from <code>Pages/Blog/_layout.razor</code>,
    which is nested inside <code>Pages/_layout.razor</code> (the outer nav above).
</aside>
@Body
